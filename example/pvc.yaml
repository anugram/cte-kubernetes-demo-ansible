---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cte-claim
  annotations:
     csi.cte.cpl.thalesgroup.com/policy: cte-csi-policy
     csi.cte.cpl.thalesgroup.com/source_pvc: local-test-claim
spec:
  storageClassName: cte-aj-sc
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Ki

